Specification：详细说明，说明书
本项目旨在把在pycharm中使用github的步骤详细记录下来，供以后参考。
1.创建远程库
    import into version control -->  github

2.本地commit(提交)
    Remark:
        本地commit之后，本地分支会领先远程分支
        如果不执行push(推送)来同步的话，本地是不会自动同步到远程的
    FAQ：
        1.如果此时，(假设本地程序员是A)程序员B异地pull远程库并修改程序
        就会有两个不同的分支，这时可以有怎样的方法使得两个人的工作合并
        并和远程库保持一致呢？
        2.执行Merge的时候有时候会有冲突，什么情况下会有冲突呢？(现在是master Branche)
        抓个活例记录下来。
